<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dataMapper">
    <select id="getTemperature" resultType="String">
      SELECT temperature
      FROM INPUTDATA
      WHERE user_id=#{user_id} AND NOT temperature=1
      ORDER BY input_data_no
      DESC LIMIT 5;
    </select>
    <select id="getHumidity" resultType="String">
        SELECT humidity
        FROM INPUTDATA
        WHERE user_id=#{user_id} AND NOT humidity=1
        ORDER BY input_data_no
        DESC LIMIT 5;
    </select>
    <select id="getDate" resultType="String">
        SELECT update_date
        FROM AVERAGEDATA
        WHERE user_id=#{user_id}
        ORDER BY average_data_no
        DESC LIMIT 1;
    </select>
</mapper>